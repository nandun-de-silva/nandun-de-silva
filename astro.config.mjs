// Astro configuration for GitHub Pages deployment
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Base site URL for GitHub Pages
  site: 'https://nandun-de-silva.github.io',
  // Base path matches the repository name
  base: '/nandun-de-silva',
});